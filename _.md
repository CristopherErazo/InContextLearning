I want to clean and refactor this codebase at the structural level considering several changes I did, because I moved this repo and the other sibling repos (Rewind and Tracklab) to a new directory structure I want to follow now:
    
    ~/research/
    ├── lib/
    │   ├── tracklab/          ← own repo
    │   └── rewind/            ← own repo
    └── projects/
        ├── in-context-learning/    
        │    ├── code/              ← this repo 
        │    └── paper/ talk/ ...   ← other potential repos for the in context learning project
        └── other-potential-projects/

What I want to do is the following:
1) Remove the src/configurations/ directory and take the code out of the src/icl/ to put it directly on src/ without an extra depth. I already have a backup of the configurations/ that later on I would like to put into lib/ to enhance organization in my code.

2) Remove runtime/ cause I dont need it

3) Remove the logs in the root and the journal.md

4) Keep only simple_config.py, rename as just config.py and take it out of the config/ directory and remove the useless directory.

5) Rename trigg_data to data and take it out of the data/ directory and remove the empty directory.

6) Rename minimal_full_rank.py to model.py and take it out of the models/ directoy and remove the empty directory.

7) Make a gitignored _scratch directory in root and put there the on_off_target/ dashboard and delete the rest of dashboards/. Put everything in scripts/ appart from dash.py and launcher.py

After all the prunning I want to refactor also the toml to build correctly the project with uv using the packages tracklab & rewind from lib/ in editable mode. 